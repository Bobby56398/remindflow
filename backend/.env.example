# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# SMTP Email Configuration (for sending reminder emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="RemindMe" <noreply@remindme.app>

# Database (SQLite - file-based, no configuration needed)
# Database file will be created automatically at backend/database.db